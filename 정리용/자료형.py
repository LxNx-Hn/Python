#파이썬의 기본자료형

#단일값(Scala Types)
#정수/실수 (int,float)
#문자,문자열(char,string
#참,거짓(bool)

#복수값(Data Structure)
#리스트     [1,2,3,4,...]
#튜플       (1,2,3,4)
#딕셔너리    {key:value,...}

#순서를 가진 모든 자료형은 인덱싱과 슬라이싱이 가능함
#인덱싱     :  Str[n]   <- n번째 데이터에 접근(호출)
#슬라이싱   : Str[n:i:k] <-n번째부터i번째 데이터까지 접근(호출) k=증가폭
# A in B  : B안에 A가 있는지 확인 (Bool값)

#import A as B
#A모듈을 가져오면서 호출자를 B로 설정함
#from A import B
#A모듈에서 B부분만 가져옴(함수,변수,클래스) , *을 넣을시 전부 가져옴